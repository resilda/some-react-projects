import React from 'react';
import ModalComponent from '../../../application-templates/shared-components/modal/modal-component';

export default function AddNewCryptoCoinModal() {
  return (
    <ModalComponent visible={true}>
      <div>Add new crypto modal</div>
    </ModalComponent>
  );
}
